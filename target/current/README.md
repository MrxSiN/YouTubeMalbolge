# Current Target

Target lock states: `UNBOUND` → `ARTIFACT_SELECTED` → `BOUND`.

Current state: **BOUND** — YouTube `21.37.42` exact bytes hashed; thirty
Verified Target Binding Set entries cover the current development features.
Production hooks remain disabled until the remaining
acceptance checks pass.

A production target may be declared only after the Binding Laboratory selects one exact
current YouTube APK/split set and produces a uniquely validated Verified Target Binding
Set.

While production is disabled:

- production hooks are disabled;
- production features are disabled;
- target mismatch/unbound state fails closed.

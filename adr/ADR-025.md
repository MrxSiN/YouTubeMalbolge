# ADR-025 — RemotePreferences is the canonical configuration transport

**Status:** Superseded by ADR-033

## Decision

Persistent configuration is stored and transported through libxposed service 102 RemotePreferences. No parallel local store, broadcast fallback, or polling mechanism is maintained.

## Consequence

All implementation, build and release tooling must preserve this decision until an ADR
explicitly supersedes it.

# ADR-026 — One classfile to R8 D8 executable backend

**Status:** Accepted in audited v4 baseline

## Decision

Validated LMP lowers to generated JVM classfiles and then standard R8/D8. No custom direct DEX backend is maintained in parallel.

## Consequence

All implementation, build and release tooling must preserve this decision until an ADR
explicitly supersedes it.

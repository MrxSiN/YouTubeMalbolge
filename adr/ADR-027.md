# ADR-027 — No post-R8 semantic DEX rewriter baseline

**Status:** Accepted in audited v4 baseline

## Decision

Baseline hardening stops short of rewriting optimized DEX after R8. Any exception requires semantic-equivalence and performance evidence.

## Consequence

All implementation, build and release tooling must preserve this decision until an ADR
explicitly supersedes it.

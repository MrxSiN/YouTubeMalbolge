# ADR-028 — Exact reference artifact target selection

**Status:** Accepted in audited v4 baseline

## Decision

Latest supported YouTube means one exact stable reference artifact set with cryptographic code-bearing identities, not a public version string.

## Consequence

All implementation, build and release tooling must preserve this decision until an ADR
explicitly supersedes it.

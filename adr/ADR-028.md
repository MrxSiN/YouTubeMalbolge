# ADR-028 — Exact reference artifact target selection

**Status:** Superseded by ADR-040

## Decision

Superseded by ADR-040. Reference artifacts remain regression fixtures, while runtime support is decided by cached structural resolution.

## Consequence

The fixture remains useful for deterministic regression validation and cheap descriptor
fallback only.

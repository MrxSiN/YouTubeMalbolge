# Capability v1

Fields:

```text
capability_id
meaning
provided_schema
required_endpoints[]
owned_state[]
consumers[]
lifecycle
```

Validator must reject capability aliases with no independent semantic value.

# Private Provenance v1

Private mapping:

```text
source_unit
→ CMG node/function
→ Logical Module Plan node
→ generated class/member
→ R8 pre/post mapping
→ final DEX location
```

Binding provenance additionally maps Endpoint → BindingSpec → evidence decision →
approved target descriptor → release binding slot.

Never package this data in the APK.

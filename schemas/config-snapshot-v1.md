# ConfigSnapshot v1

Immutable runtime-ready values only.

```text
schema_version
generation
feature_flags
primitive_options
precomputed_values
snapshot_hash
```

No storage handles, parsers, mutable collections, or resolver state.

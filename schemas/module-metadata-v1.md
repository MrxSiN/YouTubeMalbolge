# Generated Module Metadata v1

Required release resources:

```text
META-INF/xposed/module.prop
META-INF/xposed/java_init.list
META-INF/xposed/scope.list
```

`module.prop` semantic values:

```text
minApiVersion = 102
targetApiVersion = 102
staticScope = true
exceptionMode = protective
autoHotReload = <false until freeze; true after hot-reload acceptance>
```

`scope.list` semantic set:

```text
{ com.google.android.youtube }
```

`java_init.list` cardinality:

```text
exactly 1 generated module entry
```

`native_init.list` cardinality:

```text
0
```

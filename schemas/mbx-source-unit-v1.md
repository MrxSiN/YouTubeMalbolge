# MBX Source Unit v1

Canonical frontend frame fields:

```text
format_version
source_profile
unit_id
declared_imports[]
declared_exports[]
typed_records[]
content_hash
end_marker
```

Evaluator limits are external toolchain inputs and part of reproducibility metadata.

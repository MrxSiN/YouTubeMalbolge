# Resolver policy

`resolver_policy.mal` emits the candidate evidence weights and minimum acceptance score consumed by the generic runtime resolver. Cache identity and lifecycle remain conventional host mechanisms; candidate policy remains Malbolge-owned.

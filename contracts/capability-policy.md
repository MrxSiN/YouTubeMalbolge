# Sparse Capability Policy

Create a capability only when it has independent architectural value.

Admission requires at least one:

1. semantic normalization/aggregation;
2. meaningful owned state/lifecycle;
3. hiding several target-specific details;
4. multiple independent feature consumers.

A one-endpoint alias is not a capability.

Capabilities expose narrow typed semantic services and never Vector/DexKit/YouTube
physical objects.

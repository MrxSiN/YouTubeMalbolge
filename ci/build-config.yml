architecture_generation: 4
architecture_revision: audited-2026-09-23
project_state: ARCHITECTURE_ONLY

authoritative_inputs:
  toolchain: toolchain/LOCKFILE
  target: target/current/target-release.lock.yml
  authority_map: architecture/authority-map.yml
  invariants: contracts/architecture-invariants.md
  freeze_gates: ci/architecture-gates.yml

# Production enablement: target/current/target-release.lock.yml
# Forbidden runtime components: ci/architecture-gates.yml and toolchain/LOCKFILE

release_policy:
  r8_full_optimization_required: true
  semantic_equivalence_required: true
  private_provenance_shipped: false
  binding_evidence_shipped: false
  historical_bindings_shipped: false
